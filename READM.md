# How to Build and Run

First, clean the build directory to force c code to be recompiled

``` bash
cargo clean
```

Then build and run the project

```bash
cargo build
cargo run
```

You should see:
```c
Triple of 7 is 21
```
